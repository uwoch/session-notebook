- branch = Ast
- main branch beim ersten commit
- zusätzlicher feature branch für zusätzliche Entwicklungen = branching
- am Ende merge von feature und main
- nach dem merge kann man die weiteren commits des feature branchs löschen
- PR = pull request
- git log --oneline = man sieht alle Änderungen ausgeschrieben
- git switch -c feature/markdown = erstellt neuen branch feature/markdown (Feature sollte immer davor stehen)
- git branch = sehen, welche branches man hat
- man will auf dem main eigentlich nicht committen, alles wird über einzelne branches programmiert und dann erst gemergt
- git branch -M umbenennen
- git switch main = auf andere branch wechseln
- Apple command +/- Sachen größer/kleiner machen
- bei neuer Repo immer auf SSH umstellen
- rm -r Ordner = Ordner löschen
- q um aus git branch wieder raus zu kommen
- Branch löschen = git branch -d feature/quizapp
